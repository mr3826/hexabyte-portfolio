version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Pre-built artifacts deployed from GitHub Actions"
    build:
      commands:
        - echo "Skipping build - using pre-built artifacts"
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []
